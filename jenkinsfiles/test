pipeline {
  agent any
  options {
    skipDefaultCheckout(true)
  }
  stages {
    stage('Prepare') {
      steps {
        cleanWs()
        checkout scm
        sh 'make init'
        sh 'make depatch'
        sh 'make patch'
      }
    }
    stage('tests') {
      steps {
        sh 'mill -j 0 tests.run.rv64default.report'
      }
    }
  }
}
